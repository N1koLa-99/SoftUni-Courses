using System;

class Program
{
    static void Main()
    {
        while (true)
        {
            string text = Console.ReadLine();
            if (text.ToLower() == "stop")
                break;
            
            Console.WriteLine(text);
        }
    }
}
